import React from 'react';

interface CardProps {
    bg: string
}

const Card = (props: CardProps) => {

    return (
        <div className={'w-[600px] h-[300px] bg-[#fdf5e2]'}>

        </div>
    )

}

export default Card;